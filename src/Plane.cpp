#include "Plane.h"
#include "config.h"

void Plane::setup() {
    y = 20;
    z = 0;
    color = SNOW;
    direction = ofRandom(1) < 0.5;
    active = true;
}

void Plane::update() {
    
}

void Plane::draw() {
    
}

